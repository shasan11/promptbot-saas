@php $form = \App\Models\WebsiteForm::where('slug', $content['form_slug'] ?? 'newsletter')->where('is_active', true)->first(); @endphp
<section class="bg-white px-6 py-12 sm:py-16"><div class="cms-container relative mx-auto overflow-hidden rounded-3xl bg-emerald-600 px-6 py-12 text-white shadow-2xl sm:px-12"><div class="pointer-events-none absolute -right-20 -top-28 h-72 w-72 rounded-full bg-white/15 blur-3xl"></div><div class="relative mx-auto max-w-3xl text-center"><h2 class="text-3xl font-bold tracking-tight sm:text-4xl">{{ $content['heading'] ?? 'Stay informed' }}</h2><p class="mt-3 text-emerald-100">{{ $content['description'] ?? '' }}</p>@if($form)<form method="POST" action="{{ route('website.forms.submit', $form) }}" class="mx-auto mt-7 flex max-w-lg flex-col gap-2 sm:flex-row">@csrf<input type="text" name="_website" tabindex="-1" autocomplete="off" class="absolute -left-[10000px]" aria-hidden="true"><input required type="email" name="email" aria-label="Email address" placeholder="you@example.com" class="min-w-0 flex-1 rounded-xl border-0 text-slate-900"><button class="rounded-xl bg-slate-950 px-5 py-3 font-bold text-white hover:bg-slate-800">{{ $content['button_label'] ?? 'Subscribe' }}</button></form>@endif</div></div></section>
