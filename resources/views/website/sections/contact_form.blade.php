@php
    $form = \App\Models\WebsiteForm::where('slug', $content['form_slug'] ?? 'general-contact')->where('is_active', true)->first();
@endphp
<span id="{{ $content['form_slug'] ?? 'general-contact' }}" class="block scroll-mt-20"></span>
<section class="bg-slate-50 py-14 sm:py-20"><div class="cms-container mx-auto grid gap-10 px-6 lg:grid-cols-[0.75fr_1.25fr] lg:gap-20"><div><p class="cms-eyebrow">Get in touch</p><h2 class="cms-heading mt-4">{{ $content['heading'] ?? 'Contact us' }}</h2>@if($content['description'] ?? null)<p class="cms-copy mt-5">{{ $content['description'] }}</p>@endif<div class="mt-8 rounded-2xl border border-slate-200 bg-white p-5 text-sm leading-6 text-slate-600"><p class="font-bold text-slate-950">What happens next?</p><p class="mt-2">We review your message and respond using the contact details you provide. No fabricated instant-response promise.</p></div></div><div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-xl shadow-slate-950/5 sm:p-9">@if(session('status'))<div class="mb-6 rounded-xl bg-emerald-50 p-4 text-sm font-medium text-emerald-800">{{ session('status') }}</div>@endif @if($form)<form method="POST" action="{{ route('website.forms.submit', $form) }}" class="grid gap-5 sm:grid-cols-2">@csrf <div class="absolute -left-[10000px] h-px w-px overflow-hidden" aria-hidden="true"><label>Website<input name="_website" tabindex="-1" autocomplete="off"></label></div>@foreach($form->fields as $field)<label class="block {{ ($field['type'] ?? '') === 'textarea' ? 'sm:col-span-2' : '' }}"><span class="text-sm font-semibold text-slate-800">{{ $field['label'] ?? ucfirst($field['name']) }}</span>@if(($field['type'] ?? '') === 'textarea')<textarea name="{{ $field['name'] }}" rows="5" @required($field['required'] ?? false) class="mt-2 w-full rounded-xl border-slate-300 focus:border-emerald-500 focus:ring-emerald-500"></textarea>@else<input type="{{ $field['type'] ?? 'text' }}" name="{{ $field['name'] }}" @required($field['required'] ?? false) class="mt-2 w-full rounded-xl border-slate-300 focus:border-emerald-500 focus:ring-emerald-500">@endif @error($field['name'])<span class="mt-1 block text-sm text-rose-600">{{ $message }}</span>@enderror</label>@endforeach<button class="cms-button sm:col-span-2 px-5 py-3.5 font-bold">Send message</button></form>@else<p class="text-slate-500">This form is currently unavailable.</p>@endif</div></div></section>
