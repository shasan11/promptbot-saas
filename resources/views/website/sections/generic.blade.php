<section class="py-14"><div class="mx-auto max-w-5xl px-6">@if($content['heading'] ?? null)<h2 class="text-3xl font-bold text-slate-950">{{ $content['heading'] }}</h2>@endif @if($content['description'] ?? null)<p class="mt-3 max-w-3xl text-slate-600">{{ $content['description'] }}</p>@endif @if(!empty($content['items']))<div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@foreach($content['items'] as $item)<div class="rounded-lg border border-slate-200 p-5"><h3 class="font-semibold">{{ $item['title'] ?? $item['name'] ?? '' }}</h3><p class="mt-2 text-sm text-slate-600">{{ $item['description'] ?? $item['quote'] ?? '' }}</p></div>@endforeach</div>@endif</div></section>
