<section class="py-16"><div class="cms-container mx-auto grid items-center gap-10 px-6 md:grid-cols-2"><div class="{{ ($content['image_position'] ?? 'right') === 'left' ? 'md:order-2' : '' }}"><h2 class="text-3xl font-bold text-slate-950">{{ $content['heading'] ?? '' }}</h2><p class="mt-4 leading-7 text-slate-600">{{ $content['description'] ?? '' }}</p>@if(($content['button_label'] ?? null) && ($content['button_url'] ?? null))<a href="{{ $content['button_url'] }}" class="cms-button mt-6 inline-block px-5 py-3 font-semibold">{{ $content['button_label'] }}</a>@endif</div>@if($content['image_url'] ?? null)<img src="{{ $content['image_url'] }}" alt="" loading="lazy" class="{{ ($content['image_position'] ?? 'right') === 'left' ? 'md:order-1' : '' }} rounded-2xl border">@endif</div></section>
