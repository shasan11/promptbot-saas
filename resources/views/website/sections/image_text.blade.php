<section class="py-14 sm:py-20"><div class="cms-container mx-auto grid items-center gap-9 px-6 lg:grid-cols-2"><div class="cms-reveal {{ ($content['image_position'] ?? 'right') === 'left' ? 'lg:order-2' : '' }}"><h2 class="cms-heading">{{ $content['heading'] ?? '' }}</h2><p class="cms-copy mt-4">{{ $content['description'] ?? '' }}</p>@if(($content['button_label'] ?? null) && ($content['button_url'] ?? null))<a href="{{ $content['button_url'] }}" class="cms-text-link mt-6 inline-flex">{{ $content['button_label'] }} <span aria-hidden="true">&rarr;</span></a>@endif</div><div class="cms-reveal {{ ($content['image_position'] ?? 'right') === 'left' ? 'lg:order-1' : '' }}" style="animation-delay:100ms">@if($content['image_url'] ?? null)<img src="{{ $content['image_url'] }}" alt="" loading="lazy" class="w-full rounded-2xl border border-slate-200 shadow-xl">@else<div class="rounded-2xl border border-slate-200 bg-slate-950 p-4 shadow-2xl"><div class="rounded-xl bg-white p-4"><div class="flex items-center justify-between"><p class="text-sm font-bold text-slate-900">Operational overview</p><span class="rounded-full bg-emerald-50 px-2 py-1 text-xs font-semibold text-emerald-700">Live</span></div><div class="mt-4 grid grid-cols-3 gap-3">@foreach(['Open tickets','SLA status','Due tasks'] as $i => $label)<div class="rounded-xl bg-slate-50 p-3"><p class="text-[10px] text-slate-500">{{ $label }}</p><p class="mt-1 text-xl font-bold text-slate-900">{{ [24, 96, 7][$i] }}{{ $i === 1 ? '%' : '' }}</p></div>@endforeach</div><div class="mt-3 space-y-2">@foreach(['New conversation assigned','Follow-up due today','Customer replied'] as $label)<div class="flex items-center gap-3 rounded-lg border border-slate-100 p-2.5"><span class="h-2 w-2 rounded-full bg-emerald-500"></span><span class="text-xs font-medium text-slate-600">{{ $label }}</span><span class="ml-auto text-[10px] text-slate-400">Now</span></div>@endforeach</div></div></div>@endif</div></div></section>
