<section class="border-y border-slate-100 bg-white py-8"><div class="cms-container mx-auto px-6 text-center">@if($content['heading'] ?? null)<h2 class="text-xs font-bold uppercase tracking-[.18em] text-slate-500">{{ $content['heading'] }}</h2>@endif<div class="mt-5 flex flex-wrap items-center justify-center gap-x-7 gap-y-3">@foreach($content['items'] ?? [] as $item)<span class="cms-reveal flex items-center gap-2 text-sm font-semibold text-slate-600" style="animation-delay:{{ $loop->index * 45 }}ms">@if($item['image_url'] ?? null)<img src="{{ $item['image_url'] }}" alt="{{ $item['name'] ?? '' }}" class="h-8 max-w-36 object-contain">@else<span class="flex h-7 w-7 items-center justify-center rounded-lg bg-emerald-50 text-emerald-600">@include('website.partials.section_icon', ['title' => $item['name'] ?? 'Capability', 'class' => 'h-3.5 w-3.5'])</span>{{ $item['name'] ?? '' }}@endif</span>@endforeach</div></div></section>
