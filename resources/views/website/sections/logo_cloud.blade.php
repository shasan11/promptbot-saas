<section class="py-12"><div class="cms-container mx-auto px-6 text-center">@if($content['heading'] ?? null)<h2 class="text-sm font-bold uppercase tracking-widest text-slate-500">{{ $content['heading'] }}</h2>@endif<div class="mt-7 flex flex-wrap items-center justify-center gap-8">@foreach($content['items'] ?? [] as $item)<a href="{{ $item['url'] ?? '#' }}" class="flex items-center gap-2 text-sm font-semibold text-slate-600">@if($item['image_url'] ?? null)<img src="{{ $item['image_url'] }}" alt="{{ $item['name'] ?? '' }}" class="h-9 max-w-36 object-contain">@else{{ $item['name'] ?? '' }}@endif</a>@endforeach</div></div></section>
