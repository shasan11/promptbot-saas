import GuestLayout from '@/Layouts/GuestLayout';
import { Head, useForm } from '@inertiajs/react';

export default function AcceptInvitation({ invitation, token, existingUser }) { const {data,setData,post,processing,errors}=useForm({name:'',password:'',password_confirmation:''}); const submit=(e)=>{e.preventDefault();post(route('portal.invitations.store',[invitation.id,token]))}; const cls='mt-1.5 w-full rounded-lg border-slate-300'; return <GuestLayout><Head title="Accept invitation" /><h1 className="text-2xl font-bold text-slate-950">Join {invitation.account.name}</h1><p className="mt-2 text-sm text-slate-500">This invitation is for {invitation.email} with the {invitation.role} account role.</p><form onSubmit={submit} className="mt-6 space-y-4">{!existingUser&&<><label className="block text-sm font-medium">Your name<input className={cls} value={data.name} onChange={(e)=>setData('name',e.target.value)} required /></label><label className="block text-sm font-medium">Create password<input type="password" className={cls} value={data.password} onChange={(e)=>setData('password',e.target.value)} required />{errors.password&&<span className="text-xs text-rose-600">{errors.password}</span>}</label><label className="block text-sm font-medium">Confirm password<input type="password" className={cls} value={data.password_confirmation} onChange={(e)=>setData('password_confirmation',e.target.value)} required /></label></>}<button disabled={processing} className="w-full rounded-lg bg-indigo-600 px-4 py-3 text-sm font-semibold text-white">{processing?'Joining…':'Accept invitation'}</button></form></GuestLayout>; }
