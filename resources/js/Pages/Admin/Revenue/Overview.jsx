import Money from '@/Components/Portal/Money';
import PageHeader from '@/Components/Superadmin/PageHeader';
import StatusBadge from '@/Components/Superadmin/StatusBadge';
import { SectionCard } from '@/Components/UI/Card';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Head } from '@inertiajs/react';

export default function Overview({currency,stats,planMix,recentPayments}){const cards=[['MRR',stats.mrr],['ARR',stats.arr],['Revenue',stats.revenue],['Outstanding',stats.outstanding],['Refunds',stats.refunds]];return <AuthenticatedLayout header={<PageHeader title="Revenue overview" subtitle="Recurring revenue is normalized by billing interval; collected revenue remains payment-based."/>}><Head title="Revenue"/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{cards.map(([k,v])=><div key={k} className="rounded-lg border bg-white p-4"><p className="text-xs font-semibold uppercase text-slate-500">{k}</p><p className="mt-2 text-xl font-bold"><Money value={v} currency={currency}/></p></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-2"><SectionCard title="Subscription health"><dl className="grid grid-cols-2 gap-4">{[['Active',stats.activeSubscriptions],['Trials',stats.trials],['Churned',stats.churnedSubscriptions],['Failed payments',stats.failedPayments]].map(([k,v])=><div key={k} className="rounded-lg bg-slate-50 p-4"><dt className="text-sm text-slate-500">{k}</dt><dd className="mt-1 text-2xl font-bold">{v}</dd></div>)}</dl></SectionCard><SectionCard title="Plan mix"><div className="space-y-3">{Object.entries(planMix).map(([name,count])=><div key={name} className="flex justify-between rounded-lg bg-slate-50 px-4 py-3"><span className="font-medium">{name}</span><span className="font-bold">{count}</span></div>)}</div></SectionCard></div><SectionCard title="Recent payments" className="mt-6"><div className="divide-y">{recentPayments.map(p=><div key={p.id} className="grid grid-cols-[1fr_auto_auto] gap-4 py-4 text-sm"><div><p className="font-semibold">{p.customer_account?.name||p.tenant?.company_name||'Unassigned'}</p><p className="text-xs text-slate-500">{p.provider_reference||p.provider}</p></div><Money value={p.amount} currency={p.currency}/><StatusBadge status={p.status}/></div>)}</div></SectionCard></AuthenticatedLayout>}
